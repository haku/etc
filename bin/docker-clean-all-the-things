#!/usr/bin/env bash
set -eu

args=( "--filter=until=72h" )

docker buildx    prune       "${args[@]}"
docker image     prune --all "${args[@]}"
docker container prune       "${args[@]}"
docker system    prune       "${args[@]}"
