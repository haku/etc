#!/bin/bash

info=$(wicd-cli --wireless -d)
if [ "$?" != "0" ] ; then
  exit 0
fi
ssid=$(echo "$info" | grep Essid | cut -d ' ' -f 2)
qual=$(echo "$info" | grep Quality | cut -d ' ' -f 2)
echo "${ssid}/${qual} "

